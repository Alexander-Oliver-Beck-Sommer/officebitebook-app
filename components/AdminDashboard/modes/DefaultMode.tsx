import React from "react";

interface DefaultModeProps {
  /** Add relevant props here. */
}

const DefaultMode: React.FC<DefaultModeProps> = (
  {
    // Add relevant stuff here.
  },
) => {
  return (
    <section>
      <h4>Hello, World!</h4>
    </section>
  );
};

export default DefaultMode;
